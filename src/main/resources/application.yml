spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/devcamp?characterEncoding=utf8&serverTimezone=Asia/Seoul
    username: root
    password: root
  freemarker:
    cache: false

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql : true

  mail:
    host: smtp.gmail.com
    port: 587
    username: whipbaek@gmail.com
    password: mdreviutndnqshdm

spring.mail.properties.mail.smtp.auth: true
spring.mail.properties.mail.smtp.starttls.enable: true